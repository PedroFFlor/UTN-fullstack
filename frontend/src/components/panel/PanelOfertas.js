
const PanelOfertas = props => {

    return (
        <>
           {/*  <form
                action="/panel/ofertas"
                method="post"
                encType="multipart/form-data"
                className="row border border-primary rounded p-2 m-1 width100"
                onSubmit={handleSubmit}>
                <div className="col-3 text-start">
                    <label className="d-inline">Titulo Ofertas:</label>
                    <input className="form-control d-inline" type="text" name="titulo" placeholder="Ingrese titulo" onChange={handleChange} />
                    <label className="d-inline">Imagen:</label>
                    <input className="form-control d-inline" type="file" name="imagen" onChange={handleChange} />
                </div>
                <div className="col-6">
                    <label className="d-block">Descripcion:</label>
                    <textarea className="form-control" name="cuerpo" defaultValue="Ingrese el texto de las ofertas." onChange={handleChange} />
                </div>
                <div className="col-3 d-flex flex-column justify-content-evenly" >
                    <button className="form-control d-block" type="submit" >Enviar</button>
                </div>
            </form> */}
            <h1>Proximamente! no me dieron los tiempos</h1>
        </>
    )
}

export default PanelOfertas;
